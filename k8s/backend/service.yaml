apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: inventario-itu
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: inventario-backend